// Main Application Module
import { authManager } from './auth.js';

// Simple app initialization
document.addEventListener('DOMContentLoaded', () => {
    console.log('Study Agent App initialized');
});